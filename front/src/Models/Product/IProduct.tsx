export interface IProduct {
  name: string;
  summary: string;
  age: string;
  aimedFor: string;
  price: string;
  image: any;
  favorited: boolean;
  category: string;
  companyName: string;
  overAge: boolean;
}
