import React from "react";

const Modal = () => {
  return (
    <div className="fixed inset-0 bg-black bg-opacity-25 backdrop-blur-sm flex items-center justify-center"></div>
  );
};

export default Modal;
